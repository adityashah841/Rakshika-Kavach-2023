import 'package:flutter/material.dart';

class AnalysisScreenProsecution extends StatelessWidget {
  const AnalysisScreenProsecution({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}