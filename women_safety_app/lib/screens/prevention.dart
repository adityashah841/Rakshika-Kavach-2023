import 'package:flutter/material.dart';

class AnalysisScreenPrevention extends StatelessWidget {
  const AnalysisScreenPrevention({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
