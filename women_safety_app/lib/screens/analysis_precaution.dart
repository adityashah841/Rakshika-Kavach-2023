import 'package:flutter/material.dart';

class AnalysisScreenPre extends StatelessWidget {
  const AnalysisScreenPre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}